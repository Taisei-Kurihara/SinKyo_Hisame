namespace InGame.Enemy
{
    /// <summary>
    /// 敵の名称enum（PlayerPrefsのint値と対応）.
    /// enum名がAddressablesのアドレスとして使用される.
    /// </summary>
    public enum EnemyName
    {
        None = 0,
        Wendigo = 1,
        // 将来の敵:
        // Goblin = 2,
        // Dragon = 3,
    }
}
