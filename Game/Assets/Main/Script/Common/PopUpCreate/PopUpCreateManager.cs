using UnityEngine;

namespace Common {
    /// <summary>
    /// YesNoを聞く為のポップアップを作る為の物。（汎用で作る必要があった）
    /// </summary>
    public class PopUpCreateManager : SingletonMonoBase<PopUpCreateManager>
    {


        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}